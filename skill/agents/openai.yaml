interface:
  display_name: "Agent Team"
  short_description: "Evaluate, dispatch, and track native Codex agents"
  default_prompt: "Use $agent-team to keep a compact context-local lane ledger without default artifacts, dispatch every ready independent lane with positive delegation value, preserve lane identity across follow-ups, contain shared failures with a canary, and return compact results to the calling workflow."
policy:
  allow_implicit_invocation: true
