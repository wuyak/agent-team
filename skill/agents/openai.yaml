interface:
  display_name: "Agent Team"
  short_description: "Evaluate, dispatch, and track native Codex agents"
  default_prompt: "Use $agent-team to identify useful delegation, select suitable roles, prepare self-contained assignments, coordinate work, and assess and integrate returned results. Delegation is optional and does not require parallel work."
policy:
  allow_implicit_invocation: true
